# Glob and entry path conventions

Type: grilling
Status: open
Blocked by: 05, 08

## Question

Tighten `(collection, id) → filesystem path` beyond “loader base + id/slug”: glob patterns, extensions (`.md` / `.mdx` / `.json` / `.yaml`), nested ids, create-entry path rules, and how this interacts with [Multi-markdown serialization](10-multi-markdown-serialization.md).
