{
	"name": "astro-dev-cms",
	"private": true,
	"description": "PROTOTYPE monorepo — authoring shell tracer bullets",
	"packageManager": "bun@1.3.12",
	"workspaces": {
		"packages": [
			"packages/fields",
			"packages/components",
			"packages/form",
			"packages/crud",
			"packages/authoring",
			"packages/routes",
			"packages/astro-template"
		],
		"catalog": {
			"typescript": "^5.9.0",
			"svelte": "^5.38.0",
			"svelte-check": "^4.3.1",
			"zod": "^4.1.5",
			"yaml": "^2.8.0",
			"@sjsf/ajv8-validator": "^3.8.1",
			"@sjsf/basic-theme": "^3.8.1",
			"@sjsf/form": "^3.8.1",
			"ajv": "^8.17.1",
			"astro": "^5.13.0",
			"@astrojs/svelte": "^7.1.0"
		}
	},
	"scripts": {
		"dev": "bun run --filter @cms/astro-template dev",
		"build": "bun run --filter @cms/astro-template build",
		"check": "tsc -p packages/routes && tsc -p packages/crud && tsc -p packages/fields && bun run --filter @cms/form check && bun run --filter @cms/components check && bun run --filter @cms/authoring check",
		"check:allowlist": "bun run packages/crud/scripts/check-write-back-contract.ts && bun run packages/authoring/scripts/check-layer-boundaries.ts && bun run packages/authoring/scripts/check-authoring-deletion.ts",
		"check:image": "bun run packages/astro-template/scripts/check-image.ts",
		"lint": "biome check .",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.0",
		"@types/node": "^24.3.0",
		"typescript": "catalog:"
	}
}
