{
	"name": "@cms/crud",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "PROTOTYPE — write mode + writers (no UI)",
	"exports": {
		".": "./src/index.ts",
		"./fetch-client": "./src/fetch-client.ts"
	},
	"dependencies": {
		"@cms/fields": "workspace:*",
		"yaml": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"typescript": "catalog:"
	},
	"scripts": {
		"check": "tsc -p ."
	}
}
