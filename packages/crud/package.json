{
	"name": "@cms/crud",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "PROTOTYPE — CMS protocol + writers (filesystem write-back behind createCmsHost)",
	"exports": {
		".": "./src/index.ts",
		"./fetch-client": "./src/fetch-client.ts",
		"./protocol": "./src/protocol.ts"
	},
	"dependencies": {
		"@cms/fields": "workspace:*",
		"yaml": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"typescript": "catalog:"
	},
	"scripts": {
		"check": "tsc -p .",
		"check:allowlist": "bun run ./scripts/check-write-back-contract.ts"
	}
}
