/**
 * PROTOTYPE — portable write-back / allowlist contract check.
 * Retained entrypoint; suite lives in `@cms/crud` harness (issue #11).
 * Run: bun run check:allowlist
 */
import "../../crud/scripts/check-write-back-contract.ts";
