---
interface Props {
	title?: string;
	body?: string;
}

const { title = "", body = "" }: Props = Astro.props;
---

<section class="block-hero">
	<h2>{title}</h2>
	{body && <p>{body}</p>}
</section>
