---
/**
 * Form spike — builders + FieldUi → JSON Schema / uiSchema → @cms/form.
 * Pass serializable schemas: live Zod meta does not survive Astro island props.
 */

import { PlaceholderChrome } from "@cms/components";
import { prototypePostsSchema, toFormSchemas } from "@cms/fields";
import { CmsForm } from "@cms/form";

const { schema, uiSchema } = toFormSchemas(prototypePostsSchema);
const value = {
	title: "Hello tracer",
	draft: true,
	body: "Pass 3 form spike value.",
};
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>Form spike (prototype)</title>
	</head>
	<body>
		<p><a href="/">← home</a></p>
		<PlaceholderChrome label="@cms/components" />
		<!-- client:only — sjsf templates break under Astro/Vite SSR (Object.defineProperty) -->
		<CmsForm
			client:only="svelte"
			title="Posts form spike"
			schema={schema}
			uiSchema={uiSchema}
			value={value}
		/>
	</body>
</html>
