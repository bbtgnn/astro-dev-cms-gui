---
/**
 * Parked: blocksLayout / resolveBlock demo (outside v1 IR algebra).
 * Sample posts JSON no longer carries a `blocks` field after ADR-0019 slice 6.
 * Kept as a residual link target so hosts can reintroduce blocks later.
 */
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>blocks demo — parked</title>
	</head>
	<body>
		<p><a href="/">← home</a></p>
		<h1>Blocks demo parked</h1>
		<p>
			<code>blocksLayout</code> / production block renderers are outside the
			closed v1 semantic algebra. Reintroduce with a later algebra expansion —
			not part of the CMS-first reference host collections.
		</p>
	</body>
</html>
