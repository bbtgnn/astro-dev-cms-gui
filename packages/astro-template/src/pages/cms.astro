---
/**
 * PROTOTYPE / SPIKE — authoring shell mount.
 * UI at /cms; JSON API remains middleware-mounted at /_cms.
 * Editor schemas + direct Svelte fields come from virtual:@cms/config (Vite),
 * not from serializing live Zod through Astro island props.
 */

import { PlaceholderChrome } from "@cms/components";
import ShellList from "../components/ShellList.svelte";
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>CMS shell (prototype)</title>
	</head>
	<body>
		<p><a href="/">← home</a></p>
		<PlaceholderChrome label="@cms/components" />
		<!-- client:only — sjsf templates break under Astro/Vite SSR -->
		<ShellList client:only="svelte" />
	</body>
</html>
